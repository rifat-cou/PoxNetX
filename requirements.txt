# ============================================================
# PoxNetX - Project Requirements
# Python 3.13 | PyTorch 2.7.1 | CUDA 11.8
# ============================================================
# GPU users: install PyTorch from the CUDA 11.8 wheel index BEFORE
# installing the rest of this file, otherwise pip may resolve a
# CPU-only build or one built against a different CUDA version:
#
#   pip install torch==2.7.1 torchvision==0.22.1 --extra-index-url https://download.pytorch.org/whl/cu118
#   pip install -r requirements.txt
# ============================================================

# ---- Core deep learning ----
torch==2.7.1
torchvision==0.22.1
timm>=1.0.0                  # architecture zoo for the 20-model transfer learning sweep

# ---- Scientific / numerical stack ----
numpy>=2.1,<3
pandas>=2.2
scipy>=1.14                  # scipy.stats.mode for ensemble majority voting
scikit-learn>=1.5            # metrics, stratified 80/20 splits

# ---- Image processing & augmentation ----
opencv-python>=4.10
pillow>=10.4                 # inference.py preprocessing: resize, centre-crop, normalise
albumentations>=1.4          # DermGAN-DDPM Stage 1 augmentation: flips, rotation, affine, noise, blur
scikit-image>=0.24           # quickshift superpixel segmentation for LIME

# ---- Explainability (dual Grad-CAM + LIME) ----
lime>=0.2.0.1
grad-cam>=1.5.4              # pytorch-grad-cam; remove if your Grad-CAM code is fully hand-rolled

# ---- Model export & inference ----
onnx>=1.16
onnxruntime>=1.19            # CheckPox deployment inference engine

# ---- Web deployment (CheckPox) ----
fastapi>=0.111
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9      # required by FastAPI for the image upload form
pymysql>=1.1                 # MySQL backend (patients, dataset_images tables)
requests>=2.32                # Groq REST API calls
# groq>=0.9                   # uncomment instead of `requests` if you're using the official Groq SDK

# ---- Experiment tracking (optional, uncomment what you use) ----
tensorboard>=2.17
# wandb>=0.17

# ---- Utilities ----
matplotlib>=3.9
tqdm>=4.66

# ---- Carried over from your draft but not clearly used in PoxNetX ----
# Uncomment only if you actually rely on these elsewhere in the project:
# torchaudio==2.7.1           # no audio component in this project
# transformers>=4.44          # no HF transformer models in the current architecture
# diffusers>=0.29             # the DDPM stage is a hand-built U-Net, not the HF diffusers pipeline
# gradio>=4.0                  # CheckPox deployment uses FastAPI + Jinja2, not Gradio
